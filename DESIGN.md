# DESIGN.md — ClipForge AI

> 視覺世界：**製片／錄音室的紙本作業系統**（cue sheet 進點表、track sheet 軌道表、call sheet 通告單、tape box 標籤）。
> 這個工具的核心資料——場景、秒數、旁白、音軌——在數位化之前就活在這些紙上；介面把它們印回來。
> 骰選紀錄：concept-seed key `ca14682d`（direction/operate），grounded #6。挑戰者未勝出。

## 世界規則（durable）

### 材質與色彩（Restrained：中性＋單一強調）

- 底色是**紙**，不是螢幕：`--paper: #F7F5F0`（暖紙白）、卡片 `--sheet: #FFFFFF`、深段落 `--ink-well: #17150F`（僅 landing 局部與 footer）。
- 墨：`--ink: #1C1A15`（近黑棕）、次級 `--ink-60: #5C574B`、`--ink-40: #8A8478`——次級文字從墨色調淡，不用純灰。
- 唯一強調色：**REC 紅 `--rec: #C8321E`**（錄音燈／紅墨印章／油性蠟筆圈註）。只用於：主要動作、進行中狀態、紅圈標註、錄製點。禁止第二強調色；禁止任何漸層裝飾。
- 髮絲線 `--rule: #D9D4C8`（1px）是主要分隔手段；不用陰影堆疊。elevation 一律用 border，唯一例外是浮層（下拉、對話框）可用一層有位移的柔和陰影 `0 8px 24px rgba(28,26,21,.12)`。頁面內要表現「抬起的紙」用**疊紙雙框**（底下墊一層 offset 2px 的 `border-rule` 紙），不用陰影。
- 影片預覽畫布內使用**客戶品牌色**（資料驅動），工具外殼永遠維持紙墨紅。

### 字體

- Display／標題：**Noto Serif TC**（中文明體襯線；Latin 同用其襯線）。頁面主標粗細 600–700，可到 clamp 大字，行高緊。
- 內文／UI：**Noto Sans TC** 400/500/700。
- Timecode、秒數、編號、檔名、狀態碼：**Chivo Mono**（只用於資料與度量，不當「科技感」裝飾）。
- 表單欄位標籤：Noto Sans TC 500、12–13px、letter-spacing 0.05em、`--ink-60`——像表格印刷欄名。不是每節都加 eyebrow；欄名只出現在真正的表單/表格欄位上。

### 版式文法

- **場景編號（S01、S02…）與 timecode（00:00–00:08）承載真實資訊**，用 mono 印刷；這是 cue sheet 的骨架，不是裝飾編號。S## 只給場景；影片（捲帶）用 R##；痛點／訊息清單用 P#/M#——每種編號對應真實的資料類型。
- 表格：hairline 橫線為主，表頭是印刷欄名；斑馬紋禁用。
- 印章／標籤語彙表達狀態：狀態章是 1px 邊框膠囊＋mono 小字（如「渲染中」「已完成」），完成＝墨色、進行＝REC 紅、排隊＝ink-40、失敗＝REC 紅實底反白。
- 圓角尺度：小控制 4px、卡片 6px、不超過 8px。不做藥丸大卡。
- 留白節奏：段落間 48–96px；緊的群組 8–12px。標題上方空間 > 下方。

### 動態

- 一個 authored moment：**REC 燈**——渲染中狀態的紅點以 1.2s 呼吸（opacity 0.35↔1）；這是全站唯一的循環動畫。
- 其餘動態只做狀態轉換：120–200ms ease-out 的 opacity/transform；進度條寬度變化 300ms。
- 不做 scroll-jacking、不做進場視差；`prefers-reduced-motion` 時停掉 REC 呼吸。

### 元件基調

- 按鈕：主要＝REC 紅實底白字；次要＝1px 墨框紙底；危險動作＝紅框紅字，按下確認才實底。皆 4px 圓角、mono 或 sans 500。
- 輸入框：紙白底、1px `--rule` 框、focus 換墨色框＋無 glow。
- 對話框／確認：像一張抽出的表單紙，標題襯線、動作靠右。
- 空狀態：印刷風格的「未填表格」——欄位骨架＋一句話＋主要動作，不放插圖。

## 每表面備註

- `/`（Persuade）：第一屏＝「一張正在被填寫的 cue sheet」實演產品機制（見 surface brief）。
- 編輯器（Operate）：左欄＝cue sheet 場景列、右欄＝當前場景檢視；預覽畫布黑底（影片本身是螢幕，這是唯一合法的「螢幕」材質）。
- 375px：表格降級為卡片式行；編輯器改上下堆疊。

## 禁止

紫色漸層、glow、玻璃擬態、密集同尺寸卡片牆、無資訊的節編號、第二強調色、深色外殼（影片畫布除外）。
