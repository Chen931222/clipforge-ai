# eslint-config-next resolves its plugins from the project root, which pnpm's
# strict node_modules layout hides. Hoist the lint toolchain so `pnpm lint` works.
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=@typescript-eslint/*
public-hoist-pattern[]=@next/eslint-plugin-next
